# AI Thinking System 설계 문서

> 버전: 0.1.0 (Phase 1 설계)
> 최종 업데이트: 2026-02-07

---

## 1. 철학

### Second Brain의 본질

> **"내가 넣은 지식을 필요한 순간에 끄집어내게 해주는 것.
> 세계의 모든 지식과 내가 가진 모든 지식을 총망라할 만큼 방대하며,
> 체계적이고 서로 연결되어 다른 지식을 만들어내기도 한다."**

### 핵심 정체성

| 질문 | 답 |
|------|-----|
| 이 앱은 무엇인가? | **생각하게 만드는 시스템** |
| 답을 주는가? | ❌ 재료만 제공 |
| 언제 개입하는가? | 사용자가 요청할 때만 |
| AI 출력은 무엇인가? | 기록이 아닌 **재료** |

### 이 앱이 아닌 것

- ❌ AI 노트 생성기
- ❌ 자동 요약 도구
- ❌ 스마트 알림 시스템
- ❌ 생산성 앱

### 이 앱인 것

- ✅ 사고 설계 도구
- ✅ On-demand Cognitive Tool
- ✅ 인지 보조 시스템

---

## 2. 역할 분리 (불변)

### 인간의 역할

- 기록의 주체
- 의미 확정자
- 지식의 소유자
- 최종 판단자

### AI의 역할

- 재료 제공자
- 연결·혼합·단순화 담당
- **의미에 대해 발언권 없음**

### 핵심 원칙

> **"AI는 지식을 만들지 않는다. AI는 사고를 가공 가능한 상태로 만든다."**

---

## 3. 시스템 원칙 (5가지 불변 규칙)

```
1. 자동 팝업 없음
2. 선제 추천 없음
3. 알림 없음
4. 요약 자동 생성 없음
5. 사용자 명령 없이는 침묵
```

**이 중 하나라도 깨지면 철학적으로 붕괴한다.**

---

## 4. AI가 제공할 수 있는 것 (4가지만)

| 유형 | 설명 | 예시 |
|------|------|------|
| **Connection** | A와 B가 왜 닿아 있는지 설명 | "이 노트와 저 노트는 같은 전제를 공유합니다" |
| **Contrast** | A와 충돌하거나 긴장 관계인 관점 | "이 노트는 저 노트와 반대 입장입니다" |
| **Combination** | A+B에서 나올 수 있는 개념 후보 | "이 둘을 결합하면 X라는 개념이 가능합니다" |
| **Compression** | 복잡한 사고를 유지한 채 구조만 정리 | "핵심 변수는 3개입니다: A, B, C" |

### AI가 절대 하면 안 되는 것

- ❌ 결론 내리기
- ❌ 주장하기
- ❌ 확정 문장 생성
- ❌ 옳고 그름 평가
- ❌ 중요도 판단

---

## 5. Thinking Commands v1

### 5.1 Connect (연결)

**의도**: 사고 재료 확장

**입력**:
- 현재 노트 ID

**출력**:
- 관련 노트 1~2개
- 각 연결 이유 (1문장)

**출력 형식**:
```json
{
  "command": "connect",
  "results": [
    {
      "noteId": "...",
      "noteTitle": "...",
      "reason": "같은 '시간 압축' 개념을 다룹니다"
    }
  ]
}
```

**금지**:
- 요약
- 중요도 판단
- 3개 이상 추천

---

### 5.2 Contrast (대조)

**의도**: 사고의 긴장 만들기

**입력**:
- 현재 노트 ID

**출력**:
- 전제가 다른 노트 1개
- "어디서 갈라지는지" 설명

**출력 형식**:
```json
{
  "command": "contrast",
  "results": [
    {
      "noteId": "...",
      "noteTitle": "...",
      "divergence": "이 노트는 'X는 필수'라고 전제하지만, 저 노트는 'X는 선택'이라고 봅니다"
    }
  ]
}
```

**금지**:
- 옳고 그름 평가
- 어느 쪽이 더 좋다는 판단

---

### 5.3 Combine (결합)

**의도**: 창발 촉발

**입력**:
- 노트 ID 2개 이상

**출력**:
- 개념 이름 후보 1~2개
- 결합 가능성 설명

**출력 형식**:
```json
{
  "command": "combine",
  "results": [
    {
      "conceptName": "맥락 기반 인출",
      "description": "A의 '필요한 순간' + B의 '자동 연결'을 결합한 개념",
      "isDraft": true
    }
  ]
}
```

**중요**:
- `isDraft: true` 항상 표시
- 확정은 사용자만 가능

**금지**:
- 확정 정의
- "이것이 답이다" 형태의 문장

---

### 5.4 Simplify (단순화)

**의도**: 사고 유지 + 구조 정리

**입력**:
- 노트 ID 또는 선택된 텍스트

**출력**:
- 계층 구조
- 핵심 변수 목록
- 제거 가능한 중복 (있다면)

**출력 형식**:
```json
{
  "command": "simplify",
  "results": {
    "structure": [
      { "level": 1, "content": "핵심 주장" },
      { "level": 2, "content": "근거 1" },
      { "level": 2, "content": "근거 2" }
    ],
    "keyVariables": ["변수A", "변수B", "변수C"],
    "redundancies": ["이 부분과 저 부분이 같은 내용"]
  }
}
```

**금지**:
- 내용 삭제
- 의미 축소
- 결론 도출

---

## 6. Context Stack (맥락 계층)

AI가 "관련 노트"를 찾을 때 사용하는 맥락 우선순위:

```
1순위: 현재 활성 노트
2순위: 최근 30분 행동 로그
       - 열람한 노트
       - 수정한 노트
       - 생성한 노트
3순위: 현재 노트의 링크 반경 1~2 hop
4순위: 시간적 근접성 (최근 작성/수정)
```

### 맥락 점수 계산

```typescript
contextScore =
  (isCurrentNote ? 100 : 0) +
  (recentViewWeight * 30) +      // 최근 30분 내 조회
  (linkDistance === 1 ? 50 : linkDistance === 2 ? 25 : 0) +
  (recencyWeight * 20)           // 수정일 기준
```

---

## 7. 저장 규칙 (가장 중요)

### 핵심 원칙

> **AI 출력은 절대 자동 저장되지 않는다.**

### 저장 가능한 것

- 사용자가 직접 작성한 문장
- 사용자가 수정·확정한 개념

### AI 출력의 상태

- 항상 **임시** 상태
- 시각적으로 **구분** (회색, 점선 테두리 등)
- 세션 종료 시 **소멸 가능**
- 사용자가 "저장" 버튼을 눌러야만 노트로 승격

### 저장 흐름

```
AI 출력 (임시)
    ↓
사용자 검토
    ↓
[저장] 버튼 클릭
    ↓
수정 가능한 에디터 열림
    ↓
사용자 확정
    ↓
새 노트 생성 또는 기존 노트에 추가
```

---

## 8. UI 설계 원칙

### 핵심: 조용한 작업대

AI는 **말하는 존재가 아니라 작업대**처럼 보여야 함.

| 요소 | 챗 UI (❌) | 작업대 UI (✅) |
|------|-----------|---------------|
| 형태 | 대화 버블 | 카드/블록/패널 |
| 톤 | 대화체 | 정보 제시 |
| 응답 | 문장 중심 | 구조 중심 |
| 상호작용 | 타이핑 | 클릭/드래그 |

### Thinking Panel

```
┌─────────────────────────────────────┐
│  🧠 Thinking                    [×] │
├─────────────────────────────────────┤
│                                     │
│  ┌─────────────────────────────┐   │
│  │ Connect                     │   │
│  │ ─────────────────────────── │   │
│  │ 📄 "시간 관리 원칙"           │   │
│  │    같은 전제를 공유합니다      │   │
│  │                    [저장]    │   │
│  └─────────────────────────────┘   │
│                                     │
│  ┌─────────────────────────────┐   │
│  │ 📄 "생산성 vs 효율성"         │   │
│  │    관련 개념을 다룹니다        │   │
│  │                    [저장]    │   │
│  └─────────────────────────────┘   │
│                                     │
└─────────────────────────────────────┘
```

### 호출 방식

- `Cmd + /` 또는 `Ctrl + /`: Thinking Panel 토글
- 노트 에디터 툴바: Thinking 버튼
- 우클릭 컨텍스트 메뉴: "Think with this"

### 카드 상태

| 상태 | 시각적 표현 |
|------|------------|
| 임시 (AI 출력) | 점선 테두리, 회색 배경 |
| 저장됨 | 실선 테두리, 흰색 배경 |
| 호버 | 미리보기 표시 |

---

## 9. 데이터 스키마 확장

### 새 모델: ThinkingSession

```prisma
model ThinkingSession {
  id        String   @id @default(cuid())
  noteId    String   // 시작 노트
  command   String   // connect | contrast | combine | simplify
  input     Json     // 입력 데이터
  output    Json     // AI 출력 (임시)
  savedIds  String[] // 저장된 결과 ID들
  createdAt DateTime @default(now())
  expiresAt DateTime // 세션 만료 시간 (24시간 후)

  note Note @relation(fields: [noteId], references: [id], onDelete: Cascade)

  @@index([noteId])
  @@index([createdAt])
  @@map("thinking_sessions")
}
```

### Note 모델에 관계 추가

```prisma
model Note {
  // 기존 필드...

  thinkingSessions ThinkingSession[]
}
```

---

## 10. API 설계

### 엔드포인트

```
POST /api/thinking/connect
POST /api/thinking/contrast
POST /api/thinking/combine
POST /api/thinking/simplify
POST /api/thinking/save
```

### 공통 요청 형식

```typescript
interface ThinkingRequest {
  noteIds: string[];        // 대상 노트 ID들
  context?: {
    recentNoteIds?: string[]; // 최근 본 노트들
  };
}
```

### 공통 응답 형식

```typescript
interface ThinkingResponse {
  sessionId: string;        // 세션 ID
  command: 'connect' | 'contrast' | 'combine' | 'simplify';
  results: ThinkingResult[];
  expiresAt: string;        // ISO 8601
}

interface ThinkingResult {
  id: string;               // 결과 ID
  type: 'note' | 'concept' | 'structure';
  content: any;             // 명령별 다른 형식
  reason: string;           // 왜 이 결과인지
  isDraft: boolean;         // 항상 true
}
```

### 저장 요청

```typescript
interface SaveThinkingRequest {
  sessionId: string;
  resultId: string;
  saveAs: 'new_note' | 'append_to_note';
  targetNoteId?: string;    // append 시 필요
  userEdits?: string;       // 사용자 수정 내용
}
```

---

## 11. 성공 기준

### 철학적 성공

> "이 앱을 열었을 때 '아, 이 생각을 더 써야겠다'라고 느끼는가"

### 측정 가능한 지표

| 지표 | 목표 | 의미 |
|------|------|------|
| Thinking 명령 사용률 | 주 3회 이상 | 기능 발견 |
| 결과 저장률 | > 30% | 재료 품질 |
| 저장 후 수정률 | > 50% | 사용자 주도권 유지 |
| 재사용률 | > 20% | 가치 인정 |

### 실패 신호

- AI 출력을 그대로 저장하는 비율 > 80% → 사용자가 생각 안 함
- Thinking 사용 후 노트 작성 감소 → 방해가 됨
- 자동 팝업 요청 증가 → 철학 오해

---

## 12. 구현 로드맵

### Phase 1: 기반 구축

| 단계 | 작업 | 상태 |
|------|------|------|
| 1.1 | 스키마 확장 (ThinkingSession) | ⬜ |
| 1.2 | Context Stack 로직 구현 | ⬜ |
| 1.3 | Connect 명령 구현 | ⬜ |
| 1.4 | Thinking Panel UI | ⬜ |
| 1.5 | 저장 기능 | ⬜ |

### Phase 2: 명령 확장

| 단계 | 작업 | 상태 |
|------|------|------|
| 2.1 | Contrast 명령 | ⬜ |
| 2.2 | Combine 명령 | ⬜ |
| 2.3 | Simplify 명령 | ⬜ |

### Phase 3: 정교화

| 단계 | 작업 | 상태 |
|------|------|------|
| 3.1 | 맥락 점수 고도화 | ⬜ |
| 3.2 | 세션 히스토리 | ⬜ |
| 3.3 | 키보드 단축키 | ⬜ |

---

## 13. 이 시스템의 차별점

### 다른 앱들

> "우리가 정리해줄게요"

### 이 앱

> "당신이 생각할 수 있게 재료를 놓아둘게요"

**이 차이는 기능이 아니라 태도다.**

---

**문서 작성**: Claude (Arch) + ChatGPT (ZERO)
**검토**: 사용자
**다음 단계**: Phase 1 구현 명세서
